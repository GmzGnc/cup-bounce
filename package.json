{
  "name": "cup-bounce",
  "version": "1.0.0",
  "description": "Cup Bounce — Phaser 3 mobile game",
  "private": true,
  "scripts": {
    "start":   "python -m http.server 8080",
    "sync":    "npx cap sync",
    "android": "npx cap open android",
    "ios":     "npx cap open ios",
    "build":   "npx cap sync && echo Done"
  },
  "dependencies": {
    "@capacitor/core":                "^6.0.0",
    "@capacitor/android":             "^6.0.0",
    "@capacitor/ios":                 "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0",
    "@capacitor/app":                 "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
